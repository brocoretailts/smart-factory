import pandas as pd\n\ndef generate_daily_report_template(file_name):\n    # Create a DataFrame with relevant columns for daily reports\n    columns = ['Date', 'Product', 'Quantity Sold', 'Revenue', 'Comments']\n    daily_report_df = pd.DataFrame(columns=columns)\n\n    # Save the template to an Excel file\n    daily_report_df.to_excel(file_name, index=False)\n\nif __name__ == '__main__':\n    generate_daily_report_template('daily_report_template.xlsx')\n